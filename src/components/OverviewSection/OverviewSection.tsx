import React, {FunctionComponent} from "react";
import {Section} from "../Section/Section";

export const OverviewSection: FunctionComponent = () => {
    return (
        <Section title={"Overview"}>
            
        </Section>
    );
};
